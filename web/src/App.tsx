import { Layout } from 'antd'
import AppLayout from './layout/AppLayout'

function App() {
  return (
    <Layout style={{ minHeight: '100vh' }}>
      <AppLayout />
    </Layout>
  )
}

export default App
